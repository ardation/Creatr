.jumbotron
.container
  .page-header
    %h1
      New to +Creator?
      %small Resend confirmation instructions
  %p.lead
    We Can't seem to find your activation token.
    %span.muted You can resend your token using the form below.
  = semantic_form_for(resource, :as => resource_name, :url => confirmation_path(resource_name), :html => { :method => :post }) do |f|
    .control-group
      = f.input :email
    = f.actions do
      = f.submit "Retry", :class => 'btn btn-primary'
      %a.btn{:href => "/features"} Cancel
